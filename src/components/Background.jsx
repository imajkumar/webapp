import styled from 'styled-components';

const Background = styled.div`
  background:#FFF;
  color: #fff;
  min-height: 100vh;
  overflow: hidden;
  position: relative;
;`

export default Background;
